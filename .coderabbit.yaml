reviews:
  auto_review:
    enabled: true
    drafts: false          # skip drafts unless explicitly requested
    branches:
      - main               # review PRs targeting main
  summary:
    max_lines: 80
  inline:
    suggestion_limit: 15
notifications:
  pull_request:
    enabled: true
